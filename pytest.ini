[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers -m "not integration"
    --tb=short
    --disable-warnings

markers =
    integration: Integration tests (require real API)
    slow: Slow tests